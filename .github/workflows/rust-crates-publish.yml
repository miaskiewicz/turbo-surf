# Publish the turbo-surf Rust library crates to crates.io on a v* tag (mirrors
# turbo-dom's publish-crate job). Requires repo secret CARGO_REGISTRY_TOKEN.
# Published in dependency order so each dependent's version dep is already live:
#   core → view → page → render → mcp.
# (turbo-surf-napi is a cdylib → ships to npm via rust-napi-prebuild.yml, not here.)
name: rust-crates-publish

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

defaults:
  run:
    working-directory: rust

jobs:
  publish-crates:
    name: publish-crates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      # BoringSSL (the default `impersonate` anti-bot backend) builds with cmake + nasm.
      - name: Install BoringSSL toolchain (nasm/cmake)
        run: sudo apt-get update && sudo apt-get install -y nasm cmake
      # cargo reads CARGO_REGISTRY_TOKEN from the env natively; `cargo publish`
      # waits for each crate to index before the next dependent publishes.
      - run: cargo publish -p turbo-surf-core
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - run: cargo publish -p turbo-surf-view
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - run: cargo publish -p turbo-surf-page
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - run: cargo publish -p turbo-surf-render
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      # turbo-surf-raster deps turbo-html2pdf-core (crates.io) + tiny-skia/ttf-parser;
      # must publish before turbo-surf-mcp, which now depends on it.
      - run: cargo publish -p turbo-surf-raster
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - run: cargo publish -p turbo-surf-mcp
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
